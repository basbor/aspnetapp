apiVersion: v1
kind: Service
metadata:
    name: "aspnetappaks-1aec"
    labels:
        app: "aspnetappaks-1aec"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aspnetappaks-1aec"